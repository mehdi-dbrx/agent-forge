{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"BrickForge","text":"<p>Build and deploy Databricks AI agents in minutes.</p> <p>BrickForge is a pip-installable tool that takes you from zero to a live, production-grade AI agent on Databricks Apps. A browser-based Setup App walks you through workspace connection, data generation, agent configuration, and one-click deployment. No code, no notebooks, no YAML.</p> <pre><code>pip install brickforge\nbrickforge\n</code></pre> <p>Beta</p> <p>BrickForge is under active development. APIs and features may change.</p>"},{"location":"#what-you-get","title":"What you get","text":"<p>A deployed Databricks App with:</p> <ul> <li>LangGraph agent backed by a Foundation Model (Claude, DBRX, Llama via serving endpoint)</li> <li>Chat UI with streaming responses, structured blocks, inline charts, action buttons</li> <li>Auto-discovered tools from your Unity Catalog schema (functions + stored procedures)</li> <li>Genie NL-to-SQL via MCP - ask data questions in natural language</li> <li>Knowledge Assistants - RAG over uploaded documents with source citations</li> <li>Vector Search - semantic document retrieval</li> <li>External APIs - REST calls via UC connections or direct HTTP</li> <li>MCP servers - connect any MCP-compatible tool server</li> <li>A2A agents - delegate to remote agents via Google A2A protocol</li> <li>Per-user memory - long-term recall backed by Lakebase</li> <li>Charts - inline bar, line, area, and pie visualizations</li> </ul>"},{"location":"#how-it-works","title":"How it works","text":"Step What happens Connect Authenticate to any Databricks workspace via one-click OAuth. Token stored in OS keychain, never on disk. Generate Describe your domain in plain English. AI generates table schemas, synthetic data, SQL functions, stored procedures, system prompts, and a knowledge base. Wire Pick a model endpoint. Connect Genie, KA, Vector Search, APIs, MCP servers, or other agents. Toggle features. Deploy One click. Bundles code + config + chat UI, deploys to Databricks Apps, auto-grants all UC permissions. Iterate Save projects, export as <code>.forge</code> bundles, share with colleagues. Push to GitHub. Clean up when done."},{"location":"#18-setup-blocks","title":"18 setup blocks","text":"<p>Every resource your agent needs, covered by a visual step:</p> <p><code>Workspace</code> <code>SQL Warehouse</code> <code>Unity Catalog</code> <code>Data Tables</code> <code>Functions</code> <code>Model Endpoint</code> <code>Agent Prompt</code> <code>Genie Space</code> <code>Agent Bricks</code> <code>Vector Search</code> <code>MCP Servers</code> <code>REST APIs</code> <code>A2A Agents</code> <code>Features</code> <code>Lakebase</code> <code>MLflow</code> <code>Deploy</code> <code>GitHub</code></p> <p>Each block follows the same pattern: choose an approach, configure, execute, done.</p>"},{"location":"#documentation","title":"Documentation","text":"Section What it covers Getting Started Install, first run, connect workspace, pick warehouse, set schema Setup Blocks All 18 blocks - choices, actions, what happens on execute Data Generation AI wizard: domain description to tables, functions, procedures, prompts Agent Tools All tool types: UC functions, Genie, KA, APIs, MCP, A2A, charts, memory Prompt System System prompt, knowledge base, generation, project-scoped prompts Deploy Bundle build, upload, app creation, grants pipeline Projects Save, load, export .forge bundles, import on different workspaces Config config.json system, no .env files, token security, flatten() Evaluation MLflow eval pipeline, LLM judge scorer, test datasets GitHub Integration Push to GitHub, own the code, vibe on top Architecture Setup App + Agent App, config flow, package structure"},{"location":"#reference","title":"Reference","text":"Section What it covers Tools Table Complete tools reference with discovery method, feature gates, source files Config Reference Full config.json schema - every field, type, env var mapping Known Issues Current known issues and workarounds"},{"location":"#links","title":"Links","text":"<ul> <li>Website: brickforge.dev</li> <li>PyPI: pypi.org/project/brickforge</li> <li>GitHub: github.com/mehdi-dbx/brickforge</li> </ul>"},{"location":"agent-tools/","title":"Agent Tools","text":"<p>The BrickForge agent runtime discovers and loads tools dynamically at startup based on <code>config.json</code>. No hardcoded tool list - everything is config-driven.</p>"},{"location":"agent-tools/#tool-types","title":"Tool types","text":""},{"location":"agent-tools/#uc-functions-auto-discovered","title":"UC Functions (auto-discovered)","text":"<p>Unity Catalog SQL functions registered in <code>tools.functions[]</code>. The agent calls them via <code>SELECT * FROM catalog.schema.func(params)</code>.</p> <ul> <li>Discovery: <code>discover_uc_function_tools()</code> in <code>brickforge/tools/tool_factory.py</code> reads function names from config, fetches UC metadata via <code>w.functions.get()</code>, and creates <code>sql_read</code> tools.</li> <li>Config: <code>tools.functions</code> array in <code>config.json</code></li> <li>Registration: Auto-appended by <code>generate_routines.py</code> after provisioning</li> </ul> <pre><code>{\n  \"tools\": {\n    \"functions\": [\n      \"catalog.schema.get_top_customers\",\n      \"catalog.schema.flight_delay_stats\"\n    ]\n  }\n}\n</code></pre>"},{"location":"agent-tools/#genie-mcp","title":"Genie (MCP)","text":"<p>Natural-language SQL via Databricks Genie spaces. The agent asks Genie a question, Genie writes SQL, queries tables, returns results.</p> <ul> <li>Discovery: Genie space IDs from <code>tools.genie_spaces[]</code></li> <li>Config: Array of Genie space IDs</li> <li>Runtime: MCP-based tool call. SQL generated by Genie is captured and logged via <code>brickforge/agent/genie_capture.py</code></li> <li>Grant: Object type <code>genie</code>, permission <code>CAN_RUN</code></li> </ul>"},{"location":"agent-tools/#knowledge-assistants","title":"Knowledge Assistants","text":"<p>RAG over documents via Databricks Knowledge Assistant endpoints.</p> <ul> <li>Discovery: <code>brickforge/tools/ka_factory.py</code> reads <code>tools.ka.{SLUG}</code> entries from config</li> <li>Config: <code>tools.ka.{SLUG}.endpoint</code> + <code>tools.ka.{SLUG}.enabled</code></li> <li>Double gate: Tool only loads if BOTH <code>bricks.KA.enabled</code> AND <code>tools.ka.{SLUG}.enabled</code> are true</li> <li>Grant: Serving endpoint with <code>CAN_QUERY</code></li> </ul> <p>Warning</p> <p>Brick toggles (<code>bricks.KA.enabled</code>) are not yet enforced at agent runtime (see Known Issues). Tools load regardless of toggle state.</p>"},{"location":"agent-tools/#vector-search","title":"Vector Search","text":"<p>Semantic retrieval via Databricks Vector Search indexes.</p> <ul> <li>Discovery: <code>tools.vector_search.index</code> + <code>tools.vector_search.endpoint</code> in config</li> <li>Config: Index name and endpoint name</li> </ul>"},{"location":"agent-tools/#external-apis","title":"External APIs","text":"<p>REST API tools via UC Connections or direct HTTP.</p> <ul> <li>Discovery: <code>brickforge/tools/api_factory.py</code> reads <code>tools.api.{SLUG}</code> entries</li> <li>Config fields: <code>conn</code> (UC connection name), <code>url</code>, <code>method</code>, <code>path</code>, <code>desc</code>, <code>params</code>, <code>header</code>, <code>enabled</code></li> <li>UC Connection: Managed credentials - the API key is stored in Unity Catalog, not in config</li> <li>Direct HTTP: URL + auth header in config</li> </ul>"},{"location":"agent-tools/#mcp-servers","title":"MCP Servers","text":"<p>External Model Context Protocol servers.</p> <ul> <li>Discovery: <code>tools.mcp.{SLUG}</code> entries in config</li> <li>Config fields: <code>url</code>, <code>header</code>, <code>enabled</code></li> <li>Runtime: Each MCP server exposes tools the agent can call</li> </ul>"},{"location":"agent-tools/#a2a-agents","title":"A2A Agents","text":"<p>Agent-to-agent connections via Google's A2A protocol.</p> <ul> <li>Discovery: <code>brickforge/tools/a2a_factory.py</code> reads <code>tools.a2a.{SLUG}</code> entries</li> <li>Config fields: <code>url</code>, <code>header</code>, <code>enabled</code></li> <li>Runtime: The remote agent becomes a callable tool</li> </ul>"},{"location":"agent-tools/#charts","title":"Charts","text":"<p>Inline chart generation in chat responses.</p> <ul> <li>Source: <code>brickforge/tools/generate_chart.py</code></li> <li>Feature gate: <code>features.CHART.enabled</code></li> <li>Runtime: Agent generates chart specs, rendered inline in the chat UI</li> </ul>"},{"location":"agent-tools/#memory","title":"Memory","text":"<p>Per-user long-term memory stored in Lakebase.</p> <ul> <li>Source: <code>brickforge/agent/memory_tools.py</code></li> <li>Feature gate: <code>features.MEMORY.enabled</code></li> <li>Storage: <code>AsyncDatabricksStore</code> backed by Lakebase (Postgres-compatible)</li> <li>Scope: Per-user - each user gets their own memory namespace</li> </ul>"},{"location":"agent-tools/#current-time","title":"Current Time","text":"<p>Simple utility tool that returns the current timestamp.</p> <ul> <li>Source: <code>brickforge/tools/get_current_time.py</code></li> <li>Always loaded: No feature gate</li> </ul>"},{"location":"agent-tools/#tool-loading-flow","title":"Tool loading flow","text":"<p>At agent startup (<code>brickforge/agent/agent.py</code>):</p> <ol> <li>Read <code>config.json</code> via <code>flatten()</code> into env vars</li> <li><code>tool_factory.py</code> iterates over config sections:<ul> <li><code>tools.functions[]</code> -&gt; UC function tools</li> <li><code>tools.genie_spaces[]</code> -&gt; Genie MCP tools</li> <li><code>tools.ka.{SLUG}</code> -&gt; KA tools (if double-gated)</li> <li><code>tools.mcp.{SLUG}</code> -&gt; MCP tools</li> <li><code>tools.api.{SLUG}</code> -&gt; API tools</li> <li><code>tools.a2a.{SLUG}</code> -&gt; A2A tools</li> </ul> </li> <li>Feature-gated tools checked against <code>features.{NAME}.enabled</code></li> <li>All tools passed to LangGraph agent as LangChain tools</li> </ol>"},{"location":"agent-tools/#enabledisable","title":"Enable/disable","text":"<p>Multi-instance tools (KA, MCP, API, A2A) have an <code>enabled</code> boolean per entry. Disabled entries are preserved in config but omitted from <code>flatten()</code> output, so the agent never sees them.</p> <pre><code>{\n  \"tools\": {\n    \"mcp\": {\n      \"weather\": { \"url\": \"https://mcp.example.com\", \"enabled\": true },\n      \"disabled-tool\": { \"url\": \"https://old.example.com\", \"enabled\": false }\n    }\n  }\n}\n</code></pre>"},{"location":"architecture/","title":"Architecture","text":"<p>BrickForge has two main components: the Setup App (local) and the Agent App (deployed).</p>"},{"location":"architecture/#setup-app","title":"Setup App","text":"<p>Runs locally on port 9000. Provides the visual DAG-based UI for configuring and deploying agents.</p> <pre><code>User (browser)\n    |\n    v\nReact frontend [port 9000]   (pre-built in brickforge/static/)\n    |\n    v\nFastAPI backend [port 9000]   (brickforge/server.py)\n    |\n    +-- Setup routes       (brickforge/routes/setup.py)\n    +-- Auth routes         (brickforge/routes/auth.py)\n    +-- Data gen routes     (brickforge/routes/gen.py)\n    +-- KA routes           (brickforge/routes/ka.py)\n    +-- Project routes      (brickforge/routes/projects.py)\n    +-- Cleanup routes      (brickforge/routes/cleanup.py)\n    |\n    v\nDatabricks workspace (via SDK + REST API)\n    |\n    +-- Unity Catalog (tables, functions, procedures)\n    +-- SQL Warehouse\n    +-- Serving endpoints (Foundation Model API)\n    +-- Genie spaces\n    +-- Knowledge Assistants\n    +-- Databricks Apps (deployed agent)\n</code></pre>"},{"location":"architecture/#entry-point","title":"Entry point","text":"<pre><code>brickforge (CLI) -&gt; brickforge/cli.py -&gt; brickforge/server.py (FastAPI + uvicorn)\n</code></pre>"},{"location":"architecture/#frontend","title":"Frontend","text":"<ul> <li>Source: <code>visual/frontend/</code> (React, TypeScript, Vite)</li> <li>Built output: <code>brickforge/static/</code> (served by FastAPI as static files)</li> <li>Key components:<ul> <li><code>SetupView.tsx</code> - DAG grid with block cards</li> <li><code>SetupDrawer.tsx</code> - right panel with block-specific UI</li> <li><code>GenTerminal</code> - SSE streaming terminal for long-running operations</li> <li><code>BridgeAuthPanel</code> - inline OAuth connection UI</li> <li><code>GitHubPanel</code> - device flow connect + repo push</li> </ul> </li> </ul>"},{"location":"architecture/#backend","title":"Backend","text":"<ul> <li>FastAPI with CORS, static file serving, lifespan management</li> <li>Routes organized by domain: <code>setup.py</code> (~1200 lines), <code>gen.py</code>, <code>auth.py</code>, <code>projects.py</code>, <code>ka.py</code>, <code>cleanup.py</code></li> <li>All long-running operations use SSE (Server-Sent Events) via <code>lib/sse.py</code></li> </ul>"},{"location":"architecture/#agent-app-deployed","title":"Agent App (deployed)","text":"<p>Runs on Databricks Apps compute. Two services in one app:</p> <pre><code>Databricks App\n    |\n    +-- MLflow AgentServer [port 8000]  (FastAPI, uvicorn)\n    |       |\n    |       +-- LangGraph agent\n    |       |     +-- LangChain tools (auto-discovered from config)\n    |       |     +-- Foundation Model endpoint (Claude, Llama, etc.)\n    |       |\n    |       +-- /invocations endpoint\n    |\n    +-- Chat UI [port 3000]  (Node.js, Express)\n            |\n            +-- React frontend (pre-built dist)\n            +-- /api/* routes (chat, history, session, config)\n</code></pre>"},{"location":"architecture/#agent-runtime","title":"Agent runtime","text":"<ul> <li><code>brickforge/agent/agent.py</code> - LangGraph agent: tool wiring, model endpoint, memory, prompt assembly</li> <li><code>brickforge/agent/start_server.py</code> - boots MLflow AgentServer + Chat UI subprocess</li> <li><code>brickforge/agent/memory_tools.py</code> - per-user memory (AsyncDatabricksStore)</li> <li><code>brickforge/agent/genie_capture.py</code> - intercepts Genie MCP calls, logs generated SQL</li> </ul>"},{"location":"architecture/#chat-ui","title":"Chat UI","text":"<ul> <li>Node.js monorepo: <code>brickforge/app/</code></li> <li><code>client/</code> - React + Vite + TypeScript + Vercel AI SDK (streaming)</li> <li><code>server/</code> - Express + TypeScript (auth, chat proxy, SSE)</li> <li><code>packages/</code> - shared libs (auth, core, db, utils, ai-sdk-providers)</li> <li>Ships both source (for customization) and pre-built dist (for zero-friction deploy)</li> <li>Trimmed from 16MB to 1.8MB by stubbing shiki, mermaid, cytoscape, katex via Vite resolve.alias</li> </ul>"},{"location":"architecture/#config-flow","title":"Config flow","text":"<pre><code>config.json (on disk, tokens stripped)\n    |\n    + tokens restored from keyring at startup\n    |\n    v\nconfig (in-memory, full with tokens)\n    |\n    +-- flatten() -&gt; flat env dict -&gt; subprocess env (build_sub_env)\n    |\n    +-- flatten() -&gt; os.environ.update() at agent boot (start_server.py)\n    |\n    +-- config.json shipped in deploy bundle (tokens stripped)\n         |\n         +-- On DBX Apps: auth via app service principal (no user tokens needed)\n</code></pre>"},{"location":"architecture/#sse-streaming-pattern","title":"SSE streaming pattern","text":"<p>All long-running operations (deploy, data gen, provisioning) use Server-Sent Events:</p> <pre><code># Backend\nasync def exec_generator(...):\n    yield sse_line(\"[+] starting...\\n\")\n    async for event in stream_subprocess(cmd, env=sub_env, cwd=PROJECT_ROOT):\n        yield event\n    yield sse_done(True)\n</code></pre> <p><code>stream_subprocess()</code> in <code>brickforge/lib/sse.py</code> wraps <code>asyncio.create_subprocess_exec</code> and yields stdout/stderr as SSE events. Includes concurrent stderr drain to prevent pipe deadlock.</p>"},{"location":"architecture/#package-structure","title":"Package structure","text":"<pre><code>brickforge/\n  __init__.py          # PACKAGE_ROOT, PROJECT_ROOT, USER_DIR, __version__\n  cli.py               # Entry point: brickforge command\n  server.py            # FastAPI app\n  routes/              # API routes\n  lib/                 # Config, env, tokens, project paths, SSE, GitHub client\n  agent/               # LangGraph agent + MLflow server\n  tools/               # Tool factories (UC functions, KA, API, A2A, MCP, charts)\n  data/                # Data gen, provisioning, demo seeds\n  deploy/              # Deploy scripts + grant scripts\n  conf/                # KA output format configs\n  app/                 # Chat UI (Node.js monorepo)\n  stash/               # Pre-built demo templates\n  eval/                # MLflow eval pipeline\n  static/              # Pre-built Setup App frontend\n  scripts/             # Bridge auth, release scripts\n  requirements.txt     # 160 pinned dependencies\n</code></pre>"},{"location":"architecture/#build-system","title":"Build system","text":""},{"location":"architecture/#pip-package-python-m-build","title":"pip package (<code>python -m build</code>)","text":"<p>Custom build hook in <code>setup.py</code>:</p> <ol> <li>Copies <code>pyproject.toml</code> into <code>brickforge/</code> (needed for deploy)</li> <li>Runs <code>npm install &amp;&amp; npm run build:client &amp;&amp; npm run build:server</code> in <code>brickforge/app/</code></li> <li>Packages everything into wheel (~2MB)</li> </ol> <p>Node.js is only needed at build time. Not at install or deploy time.</p>"},{"location":"architecture/#pypi-release","title":"PyPI release","text":"<pre><code>scripts/release/bump.sh    # bump version\nscripts/release/build.sh   # python -m build --no-isolation\nscripts/release/upload.sh  # twine upload\n</code></pre>"},{"location":"config/","title":"Configuration","text":"<p>All configuration lives in a single <code>config.json</code> file. There are no <code>.env</code> files anywhere in BrickForge.</p>"},{"location":"config/#where-configjson-lives","title":"Where config.json lives","text":"Mode Location pip install <code>~/.brickforge/config.json</code> Editable install (<code>pip install -e .</code>) <code>PROJECT_ROOT/config.json</code> (repo root) Deployed (Databricks App) <code>./config.json</code> inside the extracted bundle"},{"location":"config/#config-providers","title":"Config providers","text":"<p>BrickForge abstracts config access behind a provider interface:</p> Provider Usage <code>LocalConfigProvider</code> Local mode. Reads/writes <code>config.json</code> on disk. <code>ForgeConfigProvider</code> Deployed mode. In-memory JSON, flushed to UC Volume as <code>config.json</code> in zip. Migrates legacy <code>config.env</code> on first read. <p>Source: <code>brickforge/lib/config_provider.py</code></p>"},{"location":"config/#structured-api","title":"Structured API","text":"<pre><code># Read\nconfig.get(\"workspace.host\")              # dot-path access\nconfig.get_section(\"tools.ka\")            # get entire section\n\n# Write\nconfig.set(\"workspace.host\", \"https://...\")\nconfig.set_many({\"workspace.host\": \"...\", \"workspace.warehouse_id\": \"...\"})\n\n# Toggle multi-instance tools\nconfig.toggle(\"tools.mcp.weather\")        # flip enabled boolean\nconfig.delete_key(\"tools.api.old-api\")    # remove entry\n</code></pre> <p>Warning</p> <p>Always use dot-paths: <code>config.get(\"workspace.host\")</code>, NOT <code>config.get(\"DATABRICKS_HOST\")</code>. The flat env-var names are only used in <code>flatten()</code> output.</p>"},{"location":"config/#flatten","title":"flatten()","text":"<p>Converts the structured JSON config into a flat dict of env-var-style key-value pairs:</p> <pre><code>flat = config.flatten()\n# Returns: {\"DATABRICKS_HOST\": \"https://...\", \"DATABRICKS_WAREHOUSE_ID\": \"abc123\", ...}\n</code></pre> <p>Used by:</p> <ul> <li><code>build_sub_env()</code> to inject config into subprocesses</li> <li><code>start_server.py</code> to set <code>os.environ</code> at agent boot</li> <li>Deploy to ship config as a file</li> </ul> <p>Disabled entries (where <code>enabled: false</code>) are omitted from <code>flatten()</code> output.</p>"},{"location":"config/#token-security","title":"Token security","text":"<p>Tokens never touch disk.</p> Mechanism How it works <code>_save()</code> strips tokens Deep-copies config data, nulls <code>workspace.token</code> and <code>model.token</code> before writing to disk. Keyring storage <code>lib/token_store.py</code> provides <code>KeyringStore</code> (macOS Keychain), <code>SecretsStore</code> (Databricks secrets), <code>NullStore</code> (fallback). Startup restore Tokens restored from keyring on app startup and project switch. GitHub tokens Also stored in keyring under <code>github.com</code> key. Deploy <code>config.json</code> in the bundle has tokens stripped. On DBX Apps, auth uses the app's service principal. <p>Source: <code>brickforge/lib/token_store.py</code></p>"},{"location":"config/#subprocess-environment","title":"Subprocess environment","text":"<p><code>build_sub_env()</code> in <code>brickforge/lib/env_utils.py</code> builds the env dict for all subprocess calls:</p> <ol> <li>Calls <code>config.flatten()</code> to get flat env vars</li> <li>Sets <code>PYTHONPATH</code> to <code>PACKAGE_ROOT</code></li> <li>Sets <code>CONFIG_FILE</code> to the absolute path of <code>config.json</code></li> <li>Sets <code>PYTHONUNBUFFERED=1</code></li> <li>Clears <code>DATABRICKS_CONFIG_FILE</code> (prevents CLI profile interference)</li> <li>Resolves auth conflicts: removes SP OAuth vars when PAT is present</li> </ol> <p>Subprocess scripts that need to write back to config use <code>lib/config_json.py</code>:</p> <pre><code>from lib.config_json import read_config, write_config\n\ncfg = read_config()                         # reads CONFIG_FILE env var\ncfg[\"tools\"][\"genie_spaces\"].append(new_id)\nwrite_config(cfg)                           # writes back to CONFIG_FILE\n</code></pre>"},{"location":"config/#_sync_env","title":"_sync_env()","text":"<p>Called on every config change and project switch:</p> <ol> <li>Clears ALL known config keys from <code>os.environ</code></li> <li>Calls <code>flatten()</code> on current config</li> <li>Sets all flattened values in <code>os.environ</code></li> </ol> <p>This prevents env var leaking between project switches.</p>"},{"location":"config/#dual-root-path-system","title":"Dual-root path system","text":"<pre><code>PACKAGE_ROOT = Path(__file__).resolve().parent              # always brickforge/\nPROJECT_ROOT = parent if pyproject.toml exists else PACKAGE_ROOT\nUSER_DIR     = Path.home() / \".brickforge\"                  # logs, config, stash cache\nLOG_FILE     = USER_DIR / \"brickforge_YYYYMMDD_HHMMSS.log\"\n</code></pre> <ul> <li>Editable install: <code>PROJECT_ROOT</code> = repo root, config at <code>PROJECT_ROOT/config.json</code></li> <li>Pip install: <code>PROJECT_ROOT</code> = <code>PACKAGE_ROOT</code> = <code>site-packages/brickforge/</code>, config at <code>~/.brickforge/config.json</code></li> </ul>"},{"location":"config/#runtime-directories","title":"Runtime directories","text":"Path Purpose <code>~/.brickforge/</code> Runtime dir: logs, config, stash cache <code>~/.brickforge/config.json</code> User config (pip install mode) <code>~/.brickforge/brickforge_*.log</code> Session log files"},{"location":"data-generation/","title":"Data Generation","text":"<p>BrickForge includes an AI-powered data generation wizard that creates your entire data layer from a plain-English domain description.</p>"},{"location":"data-generation/#the-wizard-flow","title":"The wizard flow","text":""},{"location":"data-generation/#step-1-describe-your-domain","title":"Step 1: Describe your domain","text":"<p>Enter a description like:</p> <p>\"E-commerce platform with customers, products, orders, and reviews.\"</p> <p>The wizard sends this to your configured Foundation Model endpoint.</p>"},{"location":"data-generation/#step-2-generate-table-schemas","title":"Step 2: Generate table schemas","text":"<p>The LLM designs table schemas - column names, types, constraints, relationships. Review and adjust before proceeding.</p> <p>Source: <code>brickforge/data/gen/schema_generator.py</code></p>"},{"location":"data-generation/#step-3-generate-synthetic-data","title":"Step 3: Generate synthetic data","text":"<p>From the schemas, the LLM generates realistic synthetic rows as CSV files. Saved to <code>projects/{name}/gen/csv/</code>.</p> <p>Source: <code>brickforge/data/gen/data_generator.py</code></p>"},{"location":"data-generation/#step-4-provision-to-unity-catalog","title":"Step 4: Provision to Unity Catalog","text":"<p>BrickForge creates Delta tables from the generated CSVs using <code>CREATE TABLE</code> DDL + CSV-to-Delta ingestion. Tables land in your configured <code>catalog.schema</code>.</p>"},{"location":"data-generation/#step-5-generate-uc-functions-and-stored-procedures","title":"Step 5: Generate UC functions and stored procedures","text":"<p>The routines wizard takes your table context and generates:</p> <ul> <li>UC functions - SQL functions that query and aggregate your data</li> <li>Stored procedures - mutation operations (inserts, updates, deletes)</li> </ul> <p>Source: <code>brickforge/data/gen/routine_schema_generator.py</code>, <code>brickforge/data/gen/routine_sql_generator.py</code></p> <p>Generated SQL files are written to <code>projects/{name}/gen/func/</code> and tracked in <code>routine_manifest.json</code>.</p>"},{"location":"data-generation/#data-sources","title":"Data sources","text":"<p>BrickForge supports three data sources, controlled by flags in <code>config.json</code>:</p> Source Config flag Directory Description Demo <code>data.use_demo_data</code> <code>brickforge/data/demo/</code> Pre-built seed data (CSV, DDL, functions, procedures). Ships with the package. Generated <code>data.use_gen_data</code> <code>projects/{name}/gen/</code> AI-generated from your domain description. Project-scoped. Uploaded (manual) (user-specified) Your own CSVs uploaded through the setup block. <p>Both demo and generated data can be active simultaneously. The provisioning scripts combine them.</p>"},{"location":"data-generation/#the-5-layer-sql-defense-system","title":"The 5-layer SQL defense system","text":"<p>Generating valid Databricks SQL from an LLM is hard. Databricks SQL has strict syntax rules the model doesn't know natively. BrickForge uses a 5-layer defense system to prevent and auto-correct errors:</p> Layer Name Location What it does 0 Hardened prompt <code>routine_sql_generator.py</code> Explicit instructions: \"Write Databricks SQL only. Be minimalist.\" + banned syntax list. 1 Knowledge base <code>databricks_sql_reference.md</code> Compact Databricks SQL spec injected into every LLM call. Auto-grows via Layer 4. 2 Sanitizer <code>_sanitize_sql()</code> Post-generation regex fixes: strips <code>SQL SECURITY INVOKER</code>, fixes <code>LIMIT</code> in params, reorders <code>DEFAULT</code> params. 3 Self-healing <code>_self_heal()</code> On provision failure: captures the SQL error, sends error + SQL back to LLM for correction, sanitizes again, retries (max 2 attempts). 4 Learning loop <code>_learn_constraint()</code> On successful self-heal: appends the new constraint to <code>databricks_sql_reference.md</code> so future generations avoid the same error."},{"location":"data-generation/#how-self-healing-works","title":"How self-healing works","text":"<pre><code>Generate SQL\n    |\n    v\nSanitize (Layer 2)\n    |\n    v\nProvision to UC\n    |\n    +-- Success -&gt; done\n    |\n    +-- Failure -&gt; capture error\n                    |\n                    v\n                Send error + SQL to LLM (Layer 3)\n                    |\n                    v\n                Sanitize again (Layer 2)\n                    |\n                    v\n                Retry provision\n                    |\n                    +-- Success -&gt; learn constraint (Layer 4)\n                    +-- Failure -&gt; retry once more or fail\n</code></pre>"},{"location":"data-generation/#generated-artifacts","title":"Generated artifacts","text":"<p>After a full wizard run, your project directory contains:</p> <pre><code>projects/{name}/gen/\n  csv/                    # Generated CSV files\n  init/                   # DDL SQL for table creation\n  func/                   # Generated UC function SQL files\n  manifest.json           # Tracks generated tables\n  routine_manifest.json   # Tracks generated routines\n  wizard-state.json       # Wizard progress state\n</code></pre>"},{"location":"data-generation/#provisioning-pipeline","title":"Provisioning pipeline","text":"<p>The full provisioning pipeline runs as a subprocess via SSE streaming:</p> <ol> <li><code>create_catalog_schema.py</code> - create catalog + schema if needed</li> <li><code>csv_to_delta.py</code> - load CSVs into Delta tables</li> <li><code>create_genie_space.py</code> - create Genie space with table context</li> <li><code>create_all_functions.py</code> - provision UC functions from <code>demo/func/</code> + <code>gen/func/</code></li> <li><code>create_all_procedures.py</code> - provision stored procedures</li> <li><code>create_lakebase.py</code> - create Lakebase instance (if enabled)</li> </ol> <p>Each step streams output back to the Setup App terminal.</p> <p>Note</p> <p>Stored procedures in Databricks SQL cannot use <code>BEGIN...END</code> compound statements via <code>%sql</code>. BrickForge uses <code>spark.sql()</code> for procedure provisioning.</p>"},{"location":"deploy/","title":"Deploy","text":"<p>Deploying an agent bundles your code, config, and chat UI into a single package, uploads it to Databricks, creates an App, and runs permission grants - all in one step.</p>"},{"location":"deploy/#pipeline-overview","title":"Pipeline overview","text":"<p>Triggered by the Deploy setup block (<code>exec-deploy-agent</code> action):</p> <pre><code>Build bundle -&gt; Upload -&gt; Create/update App -&gt; Wait for startup -&gt; Run grants\n</code></pre> <p>The entire flow streams output back to the Setup App terminal via SSE.</p>"},{"location":"deploy/#step-1-build-the-agent-bundle","title":"Step 1: Build the agent bundle","text":"<p><code>build_agent_bundle()</code> in <code>brickforge/deploy/deploy_agent_app.py</code> creates a zip containing:</p> Content Source Agent runtime <code>brickforge/agent/</code> Tools <code>brickforge/tools/</code> Data scripts <code>brickforge/data/</code> Config directory <code>brickforge/conf/</code> Eval scripts <code>brickforge/eval/</code> Chat UI (client) <code>brickforge/app/client/dist.tar.gz</code> Chat UI (server) <code>brickforge/app/server/dist.tar.gz</code> Project prompts <code>projects/{name}/prompt/</code> Project gen data <code>projects/{name}/gen/</code> <code>config.json</code> Full structured config (tokens stripped) <code>pyproject.toml</code> For <code>pip install .</code> on DBX Apps <code>requirements.txt</code> 160 pinned dependencies <code>app.yaml</code> Minimal - startup command + 6 runtime constants <code>databricks.yml</code> Resource permissions (reference only) <p>Warning</p> <p><code>databricks.yml</code> is generated and included but never consumed by the deploy. BrickForge uses <code>w.apps.create()</code> / <code>w.apps.deploy()</code> (Databricks SDK), NOT <code>databricks bundle deploy</code> (DAB CLI). The YAML exists for documentation only.</p>"},{"location":"deploy/#what-ships-in-appyaml","title":"What ships in app.yaml","text":"<p>The <code>app.yaml</code> contains only the startup command and a handful of runtime constants. All real config lives in <code>config.json</code> as a file in the bundle. Zero user env vars in <code>app.yaml</code>.</p>"},{"location":"deploy/#step-2-upload","title":"Step 2: Upload","text":"<p>The bundle is uploaded to the workspace via:</p> <pre><code>w.workspace.import_(format=ImportFormat.RAW, ...)\n</code></pre>"},{"location":"deploy/#step-3-create-or-update-the-app","title":"Step 3: Create or update the App","text":"<pre><code># First deploy\nw.apps.create(name=app_name, ...)\n\n# Subsequent deploys\nw.apps.deploy(app_name=app_name, ...)\n</code></pre>"},{"location":"deploy/#step-4-startsh","title":"Step 4: start.sh","text":"<p>BrickForge generates a <code>start.sh</code> script that runs on Databricks Apps compute:</p> <pre><code>unzip _bundle.dat\ntar xzf app/client/dist.tar.gz -C app/client/\ntar xzf app/server/dist.tar.gz -C app/server/\npython -m venv .venv --clear &amp;&amp; . .venv/bin/activate\npip install -r requirements.txt\nexec python -c \"from agent.start_server import main; main()\"\n</code></pre> <p>At boot, <code>start_server.py</code>:</p> <ol> <li>Reads <code>config.json</code></li> <li>Calls <code>flatten()</code> to convert JSON to flat env vars</li> <li>Calls <code>os.environ.update()</code> with the flattened dict</li> <li>Starts MLflow AgentServer on port 8000</li> <li>Starts the Chat UI (Node.js Express) on port 3000</li> </ol>"},{"location":"deploy/#step-5-post-deploy-grants","title":"Step 5: Post-deploy grants","text":"<p>After the app starts, <code>run_all_grants.py</code> automatically grants permissions to the app's service principal. The pattern:</p> <pre><code>app = w.apps.get(app_name)\nsp_id = app.service_principal_client_id\n\nw.permissions.update(\n    request_object_type=\"&lt;type&gt;\",\n    request_object_id=resource_id,\n    access_control_list=[iam.AccessControlRequest(\n        service_principal_name=sp_id,\n        permission_level=iam.PermissionLevel.&lt;LEVEL&gt;,\n    )],\n)\n</code></pre>"},{"location":"deploy/#grants-table","title":"Grants table","text":"Resource Object type Permission Script SQL Warehouse <code>warehouses</code> <code>CAN_USE</code> <code>deploy/grant/authorize_warehouse_for_app.py</code> Genie space <code>genie</code> <code>CAN_RUN</code> <code>deploy/grant/authorize_genie_for_app.py</code> Serving endpoint <code>serving-endpoints</code> <code>CAN_QUERY</code> <code>deploy/grant/authorize_endpoint_for_app.py</code> UC tables SQL <code>GRANT SELECT</code> - <code>deploy/grant/grant_app_tables.py</code> UC functions SQL <code>GRANT EXECUTE</code> - <code>deploy/grant/grant_app_functions.py</code> Lakebase SDK-specific - <code>deploy/grant/grant_lakebase_for_app.py</code> <p>Warning</p> <p>Genie spaces use object type <code>\"genie\"</code> - not <code>\"genie/space\"</code> or <code>\"genie-spaces\"</code>. This is a common SDK gotcha.</p>"},{"location":"deploy/#redeploying","title":"Redeploying","text":"<p>Redeploy follows the same pipeline. BrickForge calls <code>w.apps.deploy()</code> on the existing app. Never stop compute - just redeploy.</p>"},{"location":"deploy/#what-the-deployed-app-looks-like","title":"What the deployed app looks like","text":"<p>Two services in one Databricks App:</p> <pre><code>Databricks App\n  |\n  +-- MLflow AgentServer (port 8000)\n  |     LangGraph agent + LangChain tools\n  |     /invocations endpoint\n  |\n  +-- Chat UI (port 3000)\n        React frontend + Express backend\n        /api/* routes (chat, history, session, config)\n</code></pre> <p>Users access the Chat UI at the app URL. The Chat UI proxies requests to the AgentServer.</p>"},{"location":"evaluation/","title":"Evaluation","text":"<p>BrickForge includes an MLflow-based evaluation pipeline for measuring agent quality.</p>"},{"location":"evaluation/#overview","title":"Overview","text":"<p>The eval system uses MLflow GenAI evaluation with a custom LLM judge scorer. You define test datasets, run the eval, and track results in MLflow experiments.</p> <p>Source: <code>brickforge/eval/run_eval.py</code>, <code>brickforge/eval/scorer.py</code></p>"},{"location":"evaluation/#components","title":"Components","text":""},{"location":"evaluation/#llm-judge-scorer","title":"LLM judge scorer","text":"<p><code>brickforge/eval/scorer.py</code> defines a custom scorer that uses your configured Foundation Model endpoint to judge agent responses.</p> <p>The scorer evaluates responses against:</p> <ul> <li>Baseline: does the response match expected output?</li> <li>Guidelines: does the response follow specified quality criteria?</li> </ul>"},{"location":"evaluation/#test-datasets","title":"Test datasets","text":"<p>Define test cases as input/expected-output pairs. The eval runner sends each input to the agent and compares the response against the expected output using the LLM judge.</p>"},{"location":"evaluation/#mlflow-tracking","title":"MLflow tracking","text":"<p>Results are logged to the MLflow experiment configured in the MLflow setup block (<code>app.mlflow_experiment_id</code>).</p> <p>Tracked metrics include:</p> <ul> <li>Per-question scores from the LLM judge</li> <li>Aggregate pass/fail rates</li> <li>Response latency</li> </ul>"},{"location":"evaluation/#running-an-evaluation","title":"Running an evaluation","text":""},{"location":"evaluation/#from-the-setup-app","title":"From the Setup App","text":"<ol> <li>Configure the MLflow setup block (create or select an experiment)</li> <li>Prepare your test dataset</li> <li>Run the eval from the setup panel</li> </ol>"},{"location":"evaluation/#from-the-command-line","title":"From the command line","text":"<pre><code>python -m brickforge.eval.run_eval\n</code></pre> <p>The script reads config from <code>config.json</code>, connects to the agent endpoint, runs all test cases, and logs results to MLflow.</p>"},{"location":"evaluation/#iteration-workflow","title":"Iteration workflow","text":"<ol> <li>Deploy your agent</li> <li>Run eval against test dataset</li> <li>Review scores in MLflow</li> <li>Adjust prompts, tools, or knowledge base</li> <li>Redeploy and re-eval</li> <li>Compare runs in MLflow experiment</li> </ol> <p>Tip</p> <p>Start with a small test dataset (5-10 questions) covering your agent's core capabilities. Expand as you iterate.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11+</li> <li>A Databricks workspace with:<ul> <li>A running SQL warehouse</li> <li>Access to Unity Catalog</li> <li>A Foundation Model API endpoint (Claude, Llama, Mixtral, etc.)</li> </ul> </li> </ul>"},{"location":"getting-started/#install","title":"Install","text":"<pre><code>pip install brickforge\n</code></pre> <p>To pin a specific version:</p> <pre><code>pip install brickforge==0.1.0a33\n</code></pre>"},{"location":"getting-started/#first-run","title":"First run","text":"<pre><code>brickforge\n</code></pre> <p>This starts the Setup App (FastAPI backend + React frontend) on port 9000. Open <code>http://localhost:9000</code> in your browser.</p> <pre><code>brickforge --version   # print version and exit\nbrickforge --help      # show CLI options\n</code></pre>"},{"location":"getting-started/#connect-your-workspace","title":"Connect your workspace","text":"<p>The first setup block is Workspace. Two options:</p>"},{"location":"getting-started/#option-a-bridge-oauth-recommended","title":"Option A: Bridge OAuth (recommended)","text":"<ol> <li>Click Bridge-Forge in the workspace block</li> <li>A shell command appears - copy and run it in your terminal</li> <li>Your browser opens for Databricks OAuth</li> <li>On success, a PAT is created (7-day TTL) and saved automatically</li> </ol> <p>The bridge flow handles everything: OAuth PKCE, PAT creation, IP whitelist, token encryption.</p>"},{"location":"getting-started/#option-b-manual-entry","title":"Option B: Manual entry","text":"<ol> <li>Click Manual in the workspace block</li> <li>Paste your workspace URL (e.g. <code>https://my-workspace.cloud.databricks.com</code>)</li> <li>Paste a Personal Access Token</li> <li>Click Save</li> </ol> <p>Tip</p> <p>BrickForge auto-prepends <code>https://</code> if you forget it. Saved workspaces are remembered for quick switching.</p>"},{"location":"getting-started/#pick-a-sql-warehouse","title":"Pick a SQL warehouse","text":"<p>The SQL Warehouse block lists all running warehouses in your workspace. Click one to select it.</p> <p>Note</p> <p>The warehouse must be in RUNNING state. BrickForge does not start stopped warehouses.</p>"},{"location":"getting-started/#set-your-unity-catalog-schema","title":"Set your Unity Catalog schema","text":"<p>The Unity Catalog block lets you:</p> <ul> <li>Select an existing <code>catalog.schema</code></li> <li>Create a new one (BrickForge creates both catalog and schema if needed)</li> </ul> <p>This schema is where all your tables, functions, and procedures land.</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<p>With workspace, warehouse, and schema configured, you can:</p> <ul> <li>Generate synthetic data with the AI wizard</li> <li>Configure agent tools (Genie, KA, APIs)</li> <li>Deploy your agent to Databricks Apps</li> </ul> <p>Work through the setup blocks top to bottom. Each block follows the same pattern: choose an approach, configure, execute, done.</p>"},{"location":"github-integration/","title":"GitHub Integration","text":"<p>BrickForge can push your entire agent project to GitHub from the Setup App. No terminal needed.</p>"},{"location":"github-integration/#oauth-device-flow","title":"OAuth Device Flow","text":"<p>BrickForge uses the GitHub OAuth Device Flow for authentication. This works from localhost with no redirect URL required.</p>"},{"location":"github-integration/#how-it-works","title":"How it works","text":"<ol> <li>Click Connect GitHub in the Source Control block</li> <li>BrickForge requests a device code from GitHub</li> <li>You see a user code and a link to <code>https://github.com/login/device</code></li> <li>Open the link, enter the code, authorize BrickForge</li> <li>BrickForge polls for the token in the background</li> <li>Token stored in keyring via <code>get_token_store().set(\"github.com\", token)</code></li> </ol> <p>Client ID: <code>Ov23liqaGLy9v7sWlVsM</code> (BrickForge GitHub OAuth App - public, not secret)</p>"},{"location":"github-integration/#api-endpoints","title":"API endpoints","text":"Endpoint Method Purpose <code>/api/github/status</code> GET Check if GitHub is connected (token in keyring) <code>/api/github/connect</code> POST Start device flow, returns user_code + verification_uri <code>/api/github/poll</code> GET Poll for token completion <code>/api/github/push</code> POST Create repo + push code (SSE streaming) <p>Source: <code>brickforge/lib/github_client.py</code></p>"},{"location":"github-integration/#what-gets-pushed","title":"What gets pushed","text":"<p>BrickForge builds an agent bundle (same as deploy) and pushes it as a git repo:</p> <ul> <li>Agent runtime code (<code>agent/</code>, <code>tools/</code>, <code>data/</code>)</li> <li>System prompt and knowledge base</li> <li>Config (<code>config.json</code> with tokens stripped)</li> <li>Chat UI source and pre-built dist</li> <li><code>pyproject.toml</code> + <code>requirements.txt</code></li> <li>Generated SQL functions and procedures</li> </ul>"},{"location":"github-integration/#what-does-not-get-pushed","title":"What does NOT get pushed","text":"<ul> <li>Workspace tokens</li> <li>Model tokens</li> <li>GitHub tokens</li> <li>Any secrets from keyring</li> </ul> <p>Note</p> <p>All tokens are stripped before push. The pushed code is safe for version control.</p>"},{"location":"github-integration/#repo-creation","title":"Repo creation","text":"<p>On first push, BrickForge:</p> <ol> <li>Creates a private repo on GitHub via the API</li> <li>Repo name defaults to the project name (can be customized)</li> <li>Initializes with a commit containing the full agent bundle</li> <li>Pushes to <code>main</code> branch</li> </ol> <p>On subsequent pushes, BrickForge creates a new commit and pushes to the existing repo.</p>"},{"location":"github-integration/#vibe-coding-on-top","title":"Vibe coding on top","text":"<p>The pushed repo is real code - Python, TypeScript, SQL. You can:</p> <ol> <li>Clone the repo</li> <li>Open it in your editor</li> <li>Modify the agent logic, tools, prompts, chat UI</li> <li>Set up CI/CD</li> <li>Deploy from your own pipeline instead of the Setup App</li> </ol> <p>BrickForge is the scaffolding. Once you push to GitHub, you graduate to your own codebase and workflow.</p>"},{"location":"projects/","title":"Projects","text":"<p>BrickForge supports multiple projects per workspace. Each project is a separate agent with its own config, data, prompts, and tools.</p>"},{"location":"projects/#project-structure","title":"Project structure","text":"<pre><code>projects/\n  .current              # Text file containing active project name\n  my-project.json       # Project config (same format as config.json)\n  my-project/           # Project-scoped artifacts\n    prompt/             # System prompt + knowledge base\n    gen/                # Generated data (CSVs, SQL, manifests)\n    conf/               # KA configs\n  another-project.json\n  another-project/\n    prompt/\n    gen/\n    conf/\n</code></pre>"},{"location":"projects/#operations","title":"Operations","text":""},{"location":"projects/#create","title":"Create","text":"<p><code>POST /api/projects</code> with <code>{ \"name\": \"my-project\" }</code></p> <ul> <li>Creates a fresh config from <code>DEFAULT_CONFIG</code> (not a snapshot of current state)</li> <li>Creates the artifact directory structure</li> <li>Switches to the new project immediately</li> </ul>"},{"location":"projects/#load-switch","title":"Load / switch","text":"<p><code>POST /api/projects/{name}/load</code></p> <ul> <li>Full replace - the entire config is swapped, not merged</li> <li>Mirror is switched before save to prevent overwriting the old project</li> <li><code>_sync_env()</code> clears ALL known config keys from <code>os.environ</code> then re-sets from new project</li> <li>Frontend does <code>window.location.reload()</code> for a clean slate</li> </ul> <p>Note</p> <p>The switch order is critical: switch mirror first, then replace config data, then save, then sync env. This prevents the old project from being overwritten with new project defaults.</p>"},{"location":"projects/#delete","title":"Delete","text":"<p><code>DELETE /api/projects/{name}</code></p> <p>Removes the config JSON and artifact directory. Cannot delete the active project.</p>"},{"location":"projects/#rename","title":"Rename","text":"<p><code>POST /api/projects/{name}/rename</code> with <code>{ \"new_name\": \"...\" }</code></p> <p>Renames config file and artifact directory. Updates <code>.current</code> if this is the active project.</p>"},{"location":"projects/#list","title":"List","text":"<p><code>GET /api/projects</code></p> <p>Returns all projects with their names and active status.</p>"},{"location":"projects/#export-forge-bundles","title":"Export (.forge bundles)","text":"<p>Export packages your entire project as a <code>.forge.zip</code> bundle:</p> <ul> <li><code>config.json</code> (tokens stripped)</li> <li><code>prompt/</code> directory (system prompt + knowledge base)</li> <li><code>gen/</code> directory (generated data, SQL, manifests)</li> <li><code>conf/</code> directory (KA configs)</li> </ul> <p>The bundle is a portable, self-contained snapshot of your agent project.</p>"},{"location":"projects/#import-forge-bundles","title":"Import (.forge bundles)","text":"<p>Import supports two modes:</p>"},{"location":"projects/#load-mode-same-workspace","title":"Load mode (same workspace)","text":"<p>Replaces the current project's config and artifacts with the bundle contents. Use when loading a bundle you previously exported from the same workspace.</p>"},{"location":"projects/#new-mode-different-workspace","title":"New mode (different workspace)","text":"<p>Creates a new project from the bundle. Clears workspace-specific values (host, token, warehouse) so you can reconnect to a different workspace. Use when loading a bundle from a colleague or deploying to production.</p>"},{"location":"projects/#project-scoped-artifacts","title":"Project-scoped artifacts","text":"<p>All generated artifacts are scoped to the active project:</p> Artifact Path Purpose System prompt <code>projects/{name}/prompt/main.prompt</code> Agent behavior instructions Knowledge base <code>projects/{name}/prompt/knowledge.base</code> Domain facts Generated CSVs <code>projects/{name}/gen/csv/</code> Synthetic data Generated DDL <code>projects/{name}/gen/init/</code> Table creation SQL Generated functions <code>projects/{name}/gen/func/</code> UC function SQL Table manifest <code>projects/{name}/gen/manifest.json</code> Tracks generated tables Routine manifest <code>projects/{name}/gen/routine_manifest.json</code> Tracks generated routines Wizard state <code>projects/{name}/gen/wizard-state.json</code> Data wizard progress <p>Warning</p> <p>There is no fallback to shared directories when <code>PROJECT_DIR</code> is set. The project directory is the only source. This prevents cross-contamination between projects.</p>"},{"location":"projects/#uc-volume-sync","title":"UC Volume sync","text":"<p>Projects can be saved to and loaded from UC Volumes for remote backup:</p> <ul> <li>Save: uploads the <code>.forge.zip</code> bundle to a UC Volume path</li> <li>Load: downloads and imports the bundle from a UC Volume</li> </ul> <p>Config: <code>env_store.catalog_volume_path</code> in <code>config.json</code>.</p>"},{"location":"projects/#first-launch","title":"First launch","text":"<p>On first launch with an empty project list, BrickForge auto-creates a <code>my-project</code> project and sets it as active.</p>"},{"location":"prompt-system/","title":"Prompt System","text":"<p>The agent's behavior is defined by three files, all project-scoped.</p>"},{"location":"prompt-system/#components","title":"Components","text":""},{"location":"prompt-system/#system-prompt-mainprompt","title":"System prompt (<code>main.prompt</code>)","text":"<p>The primary instruction set for the agent. Contains:</p> <ul> <li>Role definition and personality</li> <li>Available tool descriptions and usage patterns</li> <li>Response formatting rules</li> <li>Domain-specific instructions</li> <li>The <code>KNOWLEDGE_BASE</code> placeholder (replaced at runtime)</li> </ul> <p>Location: <code>projects/{name}/prompt/main.prompt</code></p>"},{"location":"prompt-system/#knowledge-base-knowledgebase","title":"Knowledge base (<code>knowledge.base</code>)","text":"<p>Domain-specific facts, rules, and reference data injected into the system prompt at runtime. This is where you put business logic the agent needs to know.</p> <p>Location: <code>projects/{name}/prompt/knowledge.base</code></p>"},{"location":"prompt-system/#user-prompt","title":"User prompt","text":"<p>The per-turn user message. Handled by the chat UI - not a file you edit.</p>"},{"location":"prompt-system/#how-knowledge_base-injection-works","title":"How KNOWLEDGE_BASE injection works","text":"<p>At agent startup, <code>brickforge/agent/agent.py</code>:</p> <ol> <li>Reads <code>main.prompt</code> from the project prompt directory</li> <li>Reads <code>knowledge.base</code> from the same directory</li> <li>Replaces the literal string <code>KNOWLEDGE_BASE</code> in the system prompt with the knowledge base content</li> <li>Passes the assembled prompt to the LangGraph agent</li> </ol> <pre><code>main.prompt (with KNOWLEDGE_BASE placeholder)\n    +\nknowledge.base (domain facts)\n    =\nFinal system prompt (sent to LLM)\n</code></pre>"},{"location":"prompt-system/#generating-prompts-with-ai","title":"Generating prompts with AI","text":"<p>The Agent Prompt setup block offers a \"Generate from domain\" option:</p> <ol> <li>BrickForge sends your table schemas, function signatures, and domain description to the LLM</li> <li>The LLM generates a tailored system prompt and knowledge base</li> <li>Files are written to <code>projects/{name}/prompt/</code></li> <li>You can review and edit before deploying</li> </ol> <p>Source: <code>brickforge/data/gen/prompt_generator.py</code></p>"},{"location":"prompt-system/#editing-manually","title":"Editing manually","text":"<p>The setup block also provides a text editor for direct editing of both files. Changes are saved to the project prompt directory immediately.</p>"},{"location":"prompt-system/#project-scoping","title":"Project scoping","text":"<p>Prompts are fully project-scoped:</p> <ul> <li>Each project has its own <code>prompt/</code> directory under <code>projects/{name}/prompt/</code></li> <li>Switching projects switches the active prompt</li> <li>Exporting a <code>.forge.zip</code> bundle includes the prompt files</li> <li>No fallback to shared/default prompts when a project directory is set</li> </ul> <p>Tip</p> <p>Keep the knowledge base factual and concise. The agent performs better with structured reference data (tables, lists, rules) than with long prose passages.</p>"},{"location":"prompt-system/#prompt-at-deploy-time","title":"Prompt at deploy time","text":"<p>When deploying, the prompt files are included in the agent bundle:</p> <ol> <li><code>build_agent_bundle()</code> copies <code>projects/{name}/prompt/</code> into the bundle</li> <li>On Databricks Apps, <code>start_server.py</code> reads prompts from the bundle path</li> <li>The <code>PROJECT_DIR</code> env var points to the project directory inside the bundle</li> </ol>"},{"location":"setup-blocks/","title":"Setup Blocks","text":"<p>The Setup App presents 18 blocks in a directed graph. Each block follows the same lifecycle:</p> <p>choose -&gt; configure -&gt; execute -&gt; done</p> <p>Green means configured. Gray means pending. Work through them top to bottom or jump to any block.</p>"},{"location":"setup-blocks/#block-reference","title":"Block reference","text":""},{"location":"setup-blocks/#host-workspace","title":"host - Workspace","text":"<p>Connect to your Databricks workspace.</p> Choice What happens Bridge-Forge OAuth PKCE flow via shell script. Creates PAT (7-day TTL), saves host + token to config. Handles IP whitelist. Manual Paste workspace URL + PAT. Saved directly to config. <p>Saved workspaces are remembered. Switch between them from a dropdown.</p>"},{"location":"setup-blocks/#warehouse-sql-warehouse","title":"warehouse - SQL Warehouse","text":"<p>Pick a running SQL warehouse for all SQL operations.</p> Choice What happens Select existing Lists running warehouses via SDK. Click to select. Warehouse ID saved to config. <p>Note</p> <p>Only warehouses in RUNNING state appear. BrickForge does not start or create warehouses.</p>"},{"location":"setup-blocks/#schema-unity-catalog","title":"schema - Unity Catalog","text":"<p>Set the target <code>catalog.schema</code> for all tables, functions, and procedures.</p> Choice What happens Select existing Browse available catalogs and schemas. Create new Enter <code>catalog.schema</code>. BrickForge creates both if they don't exist via <code>CREATE CATALOG IF NOT EXISTS</code> / <code>CREATE SCHEMA IF NOT EXISTS</code>."},{"location":"setup-blocks/#tables-data-tables","title":"tables - Data Tables","text":"<p>Provision Delta tables in Unity Catalog.</p> Choice What happens Generate Synthetic Data Opens the data generation wizard. AI creates table schemas, generates CSV data, provisions to UC. Use Demo Data Provisions pre-built seed data from <code>brickforge/data/demo/csv/</code>. Upload CSVs Upload your own CSV files. BrickForge creates Delta tables from them. Connect Existing Point to tables already in Unity Catalog."},{"location":"setup-blocks/#functions-uc-functions","title":"functions - UC Functions","text":"<p>Create SQL functions and stored procedures in Unity Catalog.</p> Choice What happens Generate with AI Opens the routines wizard. AI generates UC functions and stored procedures from your table context. Includes 5-layer self-healing. Use Demo Functions Provisions pre-built SQL functions from <code>brickforge/data/demo/func/</code> and procedures from <code>brickforge/data/demo/proc/</code>. <p>Function names are auto-registered in config under <code>tools.functions[]</code>. The agent discovers them at runtime.</p>"},{"location":"setup-blocks/#model-model-endpoint","title":"model - Model Endpoint","text":"<p>Configure the Foundation Model API endpoint the agent uses for reasoning.</p> Choice What happens Same workspace Auto-detect scans your workspace for available serving endpoints (Claude, Llama, Mixtral, etc.). Click to select. Cross-workspace Enter a serving endpoint URL + token from a different workspace."},{"location":"setup-blocks/#prompt-agent-prompt","title":"prompt - Agent Prompt","text":"<p>Edit the system prompt and knowledge base that define agent behavior.</p> Choice What happens Generate from domain AI generates a system prompt and knowledge base tailored to your tables and domain. Edit manually Open the prompt editor. Edit <code>main.prompt</code> and <code>knowledge.base</code> directly. <p>Prompts are project-scoped - stored in <code>projects/{name}/prompt/</code>.</p>"},{"location":"setup-blocks/#genie-genie-space","title":"genie - Genie Space","text":"<p>Configure a Genie space for natural-language SQL. The agent asks Genie questions, Genie writes SQL, queries your tables, returns results.</p> Choice What happens Select existing Lists Genie spaces in your workspace. Click to select. Create new BrickForge creates a Genie space with your tables, writes the Genie room config. <p>Multiple Genie spaces supported. IDs stored in <code>tools.genie_spaces[]</code>.</p>"},{"location":"setup-blocks/#bricks-agent-bricks","title":"bricks - Agent Bricks","text":"<p>Toggleable AI building blocks that extend the agent.</p> Brick What it does KA (Knowledge Assistant) RAG over documents via a Databricks Knowledge Assistant endpoint. Info Extraction Structured data extraction from unstructured text. Doc Parsing Document parsing and analysis. Text Classification Text categorization and labeling. <p>Each brick is a toggle. Enable/disable in config under <code>bricks.{BRICK}.enabled</code>.</p>"},{"location":"setup-blocks/#vs-vector-search","title":"vs - Vector Search","text":"<p>Configure a Databricks Vector Search index for semantic retrieval.</p> Choice What happens Configure Set vector search index name and endpoint. Saved to <code>tools.vector_search</code>."},{"location":"setup-blocks/#mcp-mcp-external","title":"mcp - MCP (External)","text":"<p>Add external MCP (Model Context Protocol) servers as agent tools.</p> Choice What happens Add server Enter MCP server URL + optional auth header. Each server becomes a callable tool. <p>Multiple servers supported. Stored in <code>tools.mcp.{SLUG}</code> with <code>enabled</code> toggle.</p>"},{"location":"setup-blocks/#api-api-external","title":"api - API (External)","text":"<p>Add REST API connections as agent tools.</p> Choice What happens Add API Configure: UC connection name, URL, method, path, description, parameters, auth header. <p>Supports UC Connections (managed credentials) or direct HTTP. Stored in <code>tools.api.{SLUG}</code>.</p>"},{"location":"setup-blocks/#a2a-a2a-agents","title":"a2a - A2A (Agents)","text":"<p>Add agent-to-agent connections via Google's A2A protocol.</p> Choice What happens Add agent Enter A2A agent URL + optional auth header. The remote agent becomes a callable tool. <p>Stored in <code>tools.a2a.{SLUG}</code>.</p>"},{"location":"setup-blocks/#features-features","title":"features - Features","text":"<p>Toggle optional agent capabilities.</p> Feature What it does MEMORY Per-user long-term memory via Lakebase (AsyncDatabricksStore). CHART Inline chart generation in chat responses. VOICE Voice input support. VISION Image/vision input support. PERSONAS Multiple agent personas. <p>Each feature stored in <code>features.{NAME}.enabled</code>.</p>"},{"location":"setup-blocks/#lakebase-lakebase","title":"lakebase - Lakebase","text":"<p>Create or configure a Lakebase (Postgres-compatible) instance.</p> Choice What happens Create new BrickForge creates a Lakebase instance via SDK. Instance name saved to config. Use existing Enter an existing instance name. <p>Used by the MEMORY feature for per-user agent memory.</p>"},{"location":"setup-blocks/#mlflow-mlflow-experiment","title":"mlflow - MLflow Experiment","text":"<p>Create or configure an MLflow experiment for agent evaluation.</p> Choice What happens Create new BrickForge creates an MLflow experiment. ID saved to <code>app.mlflow_experiment_id</code>. Use existing Enter an existing experiment ID."},{"location":"setup-blocks/#deploy-deploy-app","title":"deploy - Deploy App","text":"<p>Bundle and deploy the agent as a Databricks App.</p> Choice What happens Deploy Now Builds bundle (agent code + config + chat UI), uploads to workspace, creates/updates Databricks App, waits for startup, runs all grants automatically. <p>See Deploy for the full pipeline.</p>"},{"location":"setup-blocks/#git-source-control","title":"git - Source Control","text":"<p>Push your agent project to GitHub.</p> Choice What happens Connect GitHub OAuth Device Flow - enter code in browser, BrickForge stores token in keyring. Push Creates a private repo (if needed), pushes agent bundle with tokens stripped. <p>See GitHub Integration for details.</p>"},{"location":"setup-blocks/#status-and-testing","title":"Status and testing","text":"<ul> <li><code>GET /api/setup/status</code> returns all block states from config</li> <li><code>GET /api/setup/test</code> runs inline test scripts per block (defined in <code>TEST_SCRIPTS</code> dict in <code>brickforge/routes/setup.py</code>)</li> <li>Test results are cached in the frontend to avoid re-testing on every click</li> </ul>"},{"location":"reference/config-reference/","title":"Config Reference","text":"<p>Complete <code>config.json</code> schema. All fields documented.</p>"},{"location":"reference/config-reference/#top-level-structure","title":"Top-level structure","text":"<pre><code>{\n  \"version\": 1,\n  \"workspace\": { ... },\n  \"model\": { ... },\n  \"app\": { ... },\n  \"tools\": { ... },\n  \"features\": { ... },\n  \"bricks\": { ... },\n  \"data\": { ... },\n  \"lakebase\": { ... },\n  \"env_store\": { ... },\n  \"genie_room\": { ... },\n  \"branding\": { ... }\n}\n</code></pre>"},{"location":"reference/config-reference/#workspace","title":"workspace","text":"<p>Databricks workspace connection details.</p> Field Type Env var Description <code>host</code> string <code>DATABRICKS_HOST</code> Workspace URL (e.g. <code>https://my-workspace.cloud.databricks.com</code>) <code>token</code> string <code>DATABRICKS_TOKEN</code> PAT or OAuth token. Never written to disk - stored in keyring. <code>config_profile</code> string <code>DATABRICKS_CONFIG_PROFILE</code> CLI config profile name (alternative to host+token) <code>refresh_token</code> string - OAuth refresh token (bridge auth) <code>token_endpoint</code> string - OAuth token endpoint URL <code>client_id</code> string - OAuth client ID <code>client_secret</code> string - OAuth client secret <code>warehouse_id</code> string <code>DATABRICKS_WAREHOUSE_ID</code> SQL warehouse ID <code>unity_catalog_schema</code> string <code>PROJECT_UNITY_CATALOG_SCHEMA</code> Target <code>catalog.schema</code>"},{"location":"reference/config-reference/#model","title":"model","text":"<p>Foundation Model endpoint configuration.</p> Field Type Env var Description <code>endpoint</code> string <code>AGENT_MODEL</code> Serving endpoint URL or name <code>token</code> string <code>AGENT_MODEL_TOKEN</code> Token for cross-workspace endpoints. Never written to disk."},{"location":"reference/config-reference/#app","title":"app","text":"<p>Deployed app settings.</p> Field Type Env var Description <code>name</code> string <code>DBX_APP_NAME</code> Databricks App name <code>mlflow_experiment_id</code> string <code>MLFLOW_EXPERIMENT_ID</code> MLflow experiment ID for eval"},{"location":"reference/config-reference/#tools","title":"tools","text":"<p>All agent tools configuration.</p>"},{"location":"reference/config-reference/#toolsfunctions","title":"tools.functions","text":"Field Type Env var Description <code>functions</code> string[] <code>PROJECT_FUNCTIONS</code> Comma-separated UC function names (fully qualified)"},{"location":"reference/config-reference/#toolsgenie_spaces","title":"tools.genie_spaces","text":"Field Type Env var Description <code>genie_spaces</code> string[] <code>PROJECT_GENIE_SPACES</code> Comma-separated Genie space IDs"},{"location":"reference/config-reference/#toolsvector_search","title":"tools.vector_search","text":"Field Type Env var Description <code>index</code> string <code>VS_INDEX</code> Vector search index name <code>endpoint</code> string <code>VS_ENDPOINT</code> Vector search endpoint name"},{"location":"reference/config-reference/#toolskaslug","title":"tools.ka.{SLUG}","text":"<p>Per-Knowledge-Assistant entry.</p> Field Type Env var Description <code>endpoint</code> string <code>PROJECT_KA_{SLUG}</code> KA serving endpoint URL <code>enabled</code> boolean - Enable/disable this KA. Also gated by <code>bricks.KA.enabled</code>."},{"location":"reference/config-reference/#toolsmcpslug","title":"tools.mcp.{SLUG}","text":"<p>Per-MCP-server entry.</p> Field Type Env var Description <code>url</code> string <code>PROJECT_MCP_{SLUG}</code> MCP server URL <code>header</code> string <code>PROJECT_MCP_{SLUG}_HEADER</code> Auth header <code>enabled</code> boolean - Enable/disable"},{"location":"reference/config-reference/#toolsapislug","title":"tools.api.{SLUG}","text":"<p>Per-API entry.</p> Field Type Env var Description <code>conn</code> string - UC Connection name (for managed credentials) <code>url</code> string <code>PROJECT_API_{SLUG}_URL</code> API base URL <code>method</code> string <code>PROJECT_API_{SLUG}_METHOD</code> HTTP method <code>path</code> string <code>PROJECT_API_{SLUG}_PATH</code> URL path <code>desc</code> string <code>PROJECT_API_{SLUG}_DESC</code> Tool description for the agent <code>params</code> string <code>PROJECT_API_{SLUG}_PARAMS</code> Parameter spec <code>header</code> string <code>PROJECT_API_{SLUG}_HEADER</code> Auth header <code>enabled</code> boolean - Enable/disable"},{"location":"reference/config-reference/#toolsa2aslug","title":"tools.a2a.{SLUG}","text":"<p>Per-A2A-agent entry.</p> Field Type Env var Description <code>url</code> string <code>PROJECT_A2A_{SLUG}</code> A2A agent URL <code>header</code> string <code>PROJECT_A2A_{SLUG}_HEADER</code> Auth header <code>enabled</code> boolean - Enable/disable"},{"location":"reference/config-reference/#features","title":"features","text":"<p>Optional agent capabilities. Each is a toggle.</p> Field Type Env var Description <code>MEMORY.enabled</code> boolean <code>PROJECT_FEATURE_MEMORY</code> Per-user long-term memory (Lakebase) <code>CHART.enabled</code> boolean <code>PROJECT_FEATURE_CHART</code> Inline chart generation <code>VOICE.enabled</code> boolean <code>PROJECT_FEATURE_VOICE</code> Voice input <code>VISION.enabled</code> boolean <code>PROJECT_FEATURE_VISION</code> Image/vision input <code>PERSONAS.enabled</code> boolean <code>PROJECT_FEATURE_PERSONAS</code> Multiple agent personas"},{"location":"reference/config-reference/#bricks","title":"bricks","text":"<p>Toggleable AI building blocks.</p> Field Type Env var Description <code>KA.enabled</code> boolean <code>PROJECT_BRICK_KA</code> Knowledge Assistant brick <code>INFO_EXTRACTION.enabled</code> boolean <code>PROJECT_BRICK_INFO_EXTRACTION</code> Information extraction brick <code>DOC_PARSING.enabled</code> boolean <code>PROJECT_BRICK_DOC_PARSING</code> Document parsing brick <code>TEXT_CLASSIFICATION.enabled</code> boolean <code>PROJECT_BRICK_TEXT_CLASSIFICATION</code> Text classification brick"},{"location":"reference/config-reference/#data","title":"data","text":"<p>Data source configuration.</p> Field Type Env var Description <code>use_demo_data</code> boolean <code>USE_DEMO_DATA</code> Include demo seed data <code>use_gen_data</code> boolean <code>USE_GEN_DATA</code> Include AI-generated data <code>stash_dir</code> string <code>STASH_DIR</code> Path to stash template directory"},{"location":"reference/config-reference/#lakebase","title":"lakebase","text":"<p>Lakebase instance configuration.</p> Field Type Env var Description <code>instance_name</code> string <code>LAKEBASE_INSTANCE</code> Lakebase instance name <code>agent_memory_schema</code> string <code>LAKEBASE_MEMORY_SCHEMA</code> Schema for agent memory tables"},{"location":"reference/config-reference/#env_store","title":"env_store","text":"<p>UC Volume backup configuration.</p> Field Type Env var Description <code>host</code> string <code>ENV_STORE_HOST</code> Workspace URL for volume access <code>token</code> string <code>ENV_STORE_TOKEN</code> Token for volume access <code>catalog_volume_path</code> string <code>ENV_STORE_VOLUME</code> UC Volume path for project backup"},{"location":"reference/config-reference/#genie_room","title":"genie_room","text":"<p>Genie room configuration (for auto-created rooms).</p> Field Type Env var Description <code>name</code> string <code>GENIE_ROOM_NAME</code> Genie room display name <code>description</code> string <code>GENIE_ROOM_DESC</code> Genie room description"},{"location":"reference/config-reference/#branding","title":"branding","text":"<p>UI branding for the deployed chat app.</p> Field Type Env var Description <code>logo_url</code> string <code>BRANDING_LOGO_URL</code> Logo URL for chat UI <code>brandfetch_api_key</code> string <code>BRANDFETCH_API_KEY</code> Brandfetch API key for logo lookup"},{"location":"reference/known-issues/","title":"Known Issues","text":"<p>Current limitations and bugs in BrickForge.</p>"},{"location":"reference/known-issues/#44-brick-toggles-not-enforced-at-runtime","title":"#44: Brick toggles not enforced at runtime","text":"<p>Status: Open</p> <p>Brick toggles (<code>bricks.KA.enabled</code>, <code>bricks.INFO_EXTRACTION.enabled</code>, etc.) are stored in config and respected by <code>flatten()</code> output, but are not enforced at agent runtime. Tools load regardless of the brick toggle state.</p> <p>Impact: Disabling a brick in the Setup App does not prevent the corresponding tools from being loaded by the agent. The toggle only affects the <code>flatten()</code> output (env vars), but some tool factories read config directly.</p> <p>Workaround: Disable individual tool entries (e.g. <code>tools.ka.{SLUG}.enabled: false</code>) instead of relying on the brick-level toggle.</p>"},{"location":"reference/known-issues/#ghost-ka-tools","title":"Ghost KA tools","text":"<p>Status: Open</p> <p>Stale <code>PROJECT_KA_*</code> entries in config (from deleted or renamed Knowledge Assistants) produce unexpected tools at agent runtime, such as <code>query_default_ka</code>.</p> <p>Impact: The agent may have tools pointing to non-existent KA endpoints, causing errors when the agent tries to call them.</p> <p>Workaround: Manually remove stale KA entries from <code>tools.ka</code> in config. Use the cleanup block to discover and remove orphaned resources.</p>"},{"location":"reference/known-issues/#uc-procedures-require-sparksql","title":"UC procedures require spark.sql()","text":"<p>Status: By design</p> <p>Databricks SQL stored procedures with <code>BEGIN...END</code> compound statements cannot be executed via <code>%sql</code> magic. BrickForge uses <code>spark.sql()</code> for procedure provisioning.</p> <p>Impact: None for normal usage. Relevant only if you are running SQL files manually outside BrickForge.</p>"},{"location":"reference/known-issues/#vpn-blocks-pypi-uploads","title":"VPN blocks PyPI uploads","text":"<p>Status: By design</p> <p>Corporate VPN SSL certificate interception blocks <code>twine upload</code> to PyPI.</p> <p>Impact: Developers must disconnect from VPN before running <code>scripts/release/upload.sh</code>.</p>"},{"location":"reference/known-issues/#chat-ui-size-optimization","title":"Chat UI size optimization","text":"<p>Status: Resolved</p> <p>The Chat UI was trimmed from 16MB to 1.8MB by stubbing heavy dependencies (shiki, mermaid, cytoscape, katex) via Vite resolve.alias. The stubs are in <code>brickforge/app/client/src/stubs/</code>.</p> <p>If you add new heavy dependencies to the Chat UI, check the bundle size and add stubs as needed.</p>"},{"location":"reference/tools-table/","title":"Tools Reference","text":"<p>Complete reference of all agent tool types in BrickForge.</p>"},{"location":"reference/tools-table/#tool-types","title":"Tool types","text":"Tool type Discovery method Config location Feature gate Source file Example UC Functions <code>discover_uc_function_tools()</code> reads <code>tools.functions[]</code>, fetches UC metadata <code>tools.functions</code> None <code>tools/tool_factory.py</code> <code>catalog.schema.get_top_customers</code> Genie (MCP) Genie space IDs from config <code>tools.genie_spaces</code> None <code>agent/agent.py</code> Genie space <code>01ef...</code> Knowledge Assistant <code>ka_factory.py</code> reads <code>tools.ka.{SLUG}</code> <code>tools.ka.{SLUG}</code> <code>bricks.KA.enabled</code> AND <code>tools.ka.{SLUG}.enabled</code> <code>tools/ka_factory.py</code> <code>tools.ka.eu_rights.endpoint</code> Vector Search Index + endpoint from config <code>tools.vector_search</code> None <code>tools/tool_factory.py</code> <code>my_index</code> on <code>vs_endpoint</code> External API <code>api_factory.py</code> reads <code>tools.api.{SLUG}</code> <code>tools.api.{SLUG}</code> <code>tools.api.{SLUG}.enabled</code> <code>tools/api_factory.py</code> REST call to weather API MCP Server MCP URL from config <code>tools.mcp.{SLUG}</code> <code>tools.mcp.{SLUG}.enabled</code> <code>agent/agent.py</code> <code>https://mcp.example.com</code> A2A Agent <code>a2a_factory.py</code> reads <code>tools.a2a.{SLUG}</code> <code>tools.a2a.{SLUG}</code> <code>tools.a2a.{SLUG}.enabled</code> <code>tools/a2a_factory.py</code> Remote agent at <code>https://a2a.example.com</code> Chart Built-in tool <code>features.CHART</code> <code>features.CHART.enabled</code> <code>tools/generate_chart.py</code> Inline bar chart in chat Memory Built-in tool <code>features.MEMORY</code> <code>features.MEMORY.enabled</code> <code>agent/memory_tools.py</code> Per-user memory store/recall Current Time Built-in tool None None (always loaded) <code>tools/get_current_time.py</code> Returns current timestamp"},{"location":"reference/tools-table/#config-examples","title":"Config examples","text":""},{"location":"reference/tools-table/#uc-functions","title":"UC Functions","text":"<pre><code>{\n  \"tools\": {\n    \"functions\": [\n      \"demo_catalog.demo_schema.get_flight_delays\",\n      \"demo_catalog.demo_schema.checkin_performance\"\n    ]\n  }\n}\n</code></pre>"},{"location":"reference/tools-table/#genie-spaces","title":"Genie spaces","text":"<pre><code>{\n  \"tools\": {\n    \"genie_spaces\": [\"01ef1234-5678-9abc-def0-123456789abc\"]\n  }\n}\n</code></pre>"},{"location":"reference/tools-table/#knowledge-assistant","title":"Knowledge Assistant","text":"<pre><code>{\n  \"tools\": {\n    \"ka\": {\n      \"eu_rights\": {\n        \"endpoint\": \"https://workspace.cloud.databricks.com/serving-endpoints/eu-rights-ka/invocations\",\n        \"enabled\": true\n      }\n    }\n  },\n  \"bricks\": {\n    \"KA\": { \"enabled\": true }\n  }\n}\n</code></pre>"},{"location":"reference/tools-table/#external-api","title":"External API","text":"<pre><code>{\n  \"tools\": {\n    \"api\": {\n      \"weather\": {\n        \"conn\": \"weather_api_conn\",\n        \"url\": \"https://api.weather.com\",\n        \"method\": \"GET\",\n        \"path\": \"/v1/forecast\",\n        \"desc\": \"Get weather forecast for a location\",\n        \"params\": \"location:string\",\n        \"header\": \"\",\n        \"enabled\": true\n      }\n    }\n  }\n}\n</code></pre>"},{"location":"reference/tools-table/#mcp-server","title":"MCP Server","text":"<pre><code>{\n  \"tools\": {\n    \"mcp\": {\n      \"slack\": {\n        \"url\": \"https://mcp-slack.example.com\",\n        \"header\": \"Bearer xxx\",\n        \"enabled\": true\n      }\n    }\n  }\n}\n</code></pre>"},{"location":"reference/tools-table/#a2a-agent","title":"A2A Agent","text":"<pre><code>{\n  \"tools\": {\n    \"a2a\": {\n      \"billing_agent\": {\n        \"url\": \"https://billing-agent.example.com\",\n        \"header\": \"\",\n        \"enabled\": true\n      }\n    }\n  }\n}\n</code></pre>"},{"location":"reference/tools-table/#tool-loading-order","title":"Tool loading order","text":"<ol> <li>UC Functions (<code>tools.functions[]</code>)</li> <li>Genie MCP (<code>tools.genie_spaces[]</code>)</li> <li>Knowledge Assistants (<code>tools.ka.*</code> - double-gated)</li> <li>Vector Search (<code>tools.vector_search</code>)</li> <li>External APIs (<code>tools.api.*</code>)</li> <li>MCP Servers (<code>tools.mcp.*</code>)</li> <li>A2A Agents (<code>tools.a2a.*</code>)</li> <li>Feature-gated tools (Chart, Memory)</li> <li>Built-in tools (Current Time)</li> </ol>"},{"location":"reference/tools-table/#grants-required-per-tool-type","title":"Grants required per tool type","text":"Tool type Grant needed Object type Permission UC Functions <code>GRANT EXECUTE</code> on functions SQL grant - Genie <code>w.permissions.update()</code> <code>genie</code> <code>CAN_RUN</code> Knowledge Assistant <code>w.permissions.update()</code> <code>serving-endpoints</code> <code>CAN_QUERY</code> External API (UC Conn) UC Connection access SQL grant - MCP Server None (external) - - A2A Agent None (external) - -"}]}